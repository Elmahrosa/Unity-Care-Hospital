<<<<<<< Updated upstream
=======
version: '3.8'

>>>>>>> Stashed changes
services:
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: uch-api
    restart: unless-stopped
    env_file: ./backend/.env
    ports:
      - "5000:5000"
    depends_on:
      mongo:
        condition: service_healthy
    networks:
      - uch-network
    volumes:
      - api-logs:/app/logs

  mongo:
    image: mongo:7
    container_name: uch-mongo
    restart: unless-stopped
    environment:
      MONGO_INITDB_DATABASE: unity_care_hospital
    volumes:
      - mongo-data:/data/db
    networks:
      - uch-network
    healthcheck:
<<<<<<< Updated upstream
      # Added --quiet to prevent log noise and ensure clean exit codes
      test: ["CMD", "mongosh", "--quiet", "--eval", "db.adminCommand('ping')"]
=======
      test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/test --quiet
>>>>>>> Stashed changes
      interval: 10s
      timeout: 10s
      retries: 5
      start_period: 40s

  website:
    image: nginx:alpine
    container_name: uch-website
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./frontend/dist:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - uch-network

networks:
  uch-network:
    driver: bridge

volumes:
  mongo-data:
  api-logs:
